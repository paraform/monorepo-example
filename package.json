{
  "private": true,
  "scripts": {
    "dev:api": "cd apps/api && yarn dev",
    "dev:app": "cd apps/app && yarn dev",
    "dev:web": "cd apps/web && yarn dev",
    "dev:all": "yarn dev:api & yarn dev:app & yarn dev:web"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "yarn@3.0.2",
  "devDependencies": {
    "next-transpile-modules": "^8.0.0"
  }
}
